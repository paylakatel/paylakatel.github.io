// title mixin
mixin project_title(title, subtitle)
  div
    h2=title
        h4=subtitle
            hr

// Image mixin
mixin image(file, alt, subtitle)
    .caption
        img(src="../projects/img/" + file alt=alt class="img-responsive")
        strong=alt
        p=subtitle
    
